/**
 * Step 13: split app/bootstrap.js into sub-factories under app/bootstrap/.
 *
 * - create-message-graph.js — circular message/input factory wiring
 * - create-app-services.js — permissions, chat reset, tabs, locale
 * - wire-app.js — global events + message bridge
 */
console.log('Step 13: bootstrap.js delegates to app/bootstrap/* sub-factories');
