/**
 * Step 10: extract bridge/app-handlers, ui/global-events; wire initMessageBridge.
 * (Applied manually — see git diff for chat-app.js changes.)
 */
console.log('Step 10: bridge/app-handlers.js, ui/global-events.js integrated in chat-app.js');
