/**
 * Step 12+: shared webview protocol types + Vitest unit tests for webview modules.
 *
 * - src/shared/webview-protocol.ts — extension-side message unions + type guards
 * - media/src/shared/webview-protocol.js — mirrored type name constants for webview
 * - media/src/tests/*.test.js — group-utils, session-state, message-bridge, protocol
 * - npm run test:webview — Vitest (separate from extension Mocha suite)
 */
console.log('Step 12+: webview-protocol + test:webview (see src/shared/webview-protocol.ts)');
